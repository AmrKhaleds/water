@extends('acp.layout.app')@section('title')    @lang('back.accounting_setting')@endsection@section('content')    <!-- start page title -->    <div class="row">        <div class="col-12">            <div class="page-title-box d-flex align-items-center justify-content-between">                <h4 class="mb-0">@lang('back.accounting_setting')</h4>                <div class="page-title-right">                    <ol class="breadcrumb m-0">                        <li class="breadcrumb-item active">@lang('back.accounting_setting')</li>                        <li class="breadcrumb-item">@lang('back.dashborad')</li>                    </ol>                </div>            </div>        </div>    </div>    <!-- end page title -->    <div class="row">        <div class="col-lg-12">            <div class="card">                @if(Session::has('msg'))                    <div class="alert alert-success alert-dismissible fade show" role="alert">                        <i class="uil uil-check me-2"></i>                        {!! session('msg') !!}                        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">                        </button>                    </div>                @endif                @if($errors->any())                    @foreach($errors->all() as $error)                        <div class="alert alert-danger alert-dismissible fade show" role="alert">                            <p></p>                            <i class="uil uil-exclamation-octagon me-2"></i>                            {{ $error }}                            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close">                            </button>                        </div>                    @endforeach                @endif                <div class="card-body">                    <form action="{{route('setting.store')}}" method="post">                        @method('POST')                        @csrf                        <div class="row gutters">                            @foreach($array_groups as $group)                                <div class="col-xl-6 col-lg-6 col-md-6 col-sm-12">                                    <div class="card">                                        <div class="card-header">                                            <div class="card-title">@lang('back.'.$group)</div>                                        </div>                                        <div class="card-body">                                            @foreach($settings->where('groupe',$group) as $setting)                                                <div class="col-xl-12 col-lg col-md-12 col-sm-12 col-12">                                                    <div class="form-group row">                                                        <label for="horizontal-Fullname-input"                                                               class="col-sm-4 col-form-label">@lang('back.'.$setting->name) *</label>                                                        <div class="col-sm-7">                                                            @if($setting->input_type == 'select')                                                                <select name="{{$setting->name}}" class="form-control  selectpicker" data-live-search="true">                                                                    <option value="">@lang('back.select_one')</option>                                                                    @foreach($accounts as $account)                                                                        <option value="{{$account->id}}" {{$account->id == $setting->value ? 'selected' : ''}} >{{$account->account_name}}, {{$account->num}}</option>                                                                    @endforeach                                                                </select>                                                            @elseif($setting->input_type == 'number')                                                                <input type="{{$setting->input_type}}" name="{{$setting->name}}" id="" class="form-control form-control-lg" value="{{$setting->value}}">                                                            @endif                                                        </div>                                                    </div>                                                </div>                                            @endforeach                                        </div>                                    </div>                                </div>                            @endforeach                        </div>                        <div class="custom-btn-group">                            <!-- Buttons -->                            <button type="submit" class="btn btn-primary">@lang('back.submit')</button>                            <button type="reset" class="btn btn-info">@lang('back.cancel')</button>                        </div>                    </form>                </div>            </div>        </div>    </div>@endsection